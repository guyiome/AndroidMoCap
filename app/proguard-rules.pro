# Add project specific ProGuard rules here.
# Minification est désactivée pour le PoC (buildTypes.release.isMinifyEnabled = false).
# Si tu l'actives plus tard, MediaPipe et ARCore nécessitent généralement des règles -keep
# spécifiques (protobuf, JNI) : voir la documentation officielle de chaque SDK avant d'activer R8.
