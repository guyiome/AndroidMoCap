# AndroidMoCap — Suivi des tests unitaires

*🇬🇧 English version: [AndroidMoCap_tests_unitaires.md](AndroidMoCap_tests_unitaires.md)*

Document de référence sur l'état courant de la couverture de tests -- pas un journal, pas d'historique de correctifs. Il liste, module par module, ce qui est couvert par des tests unitaires JVM purs (`app/src/test/`, exécutés via `./gradlew testDebugUnitTest`, aucun appareil/émulateur requis), et pourquoi le reste ne l'est pas.

Deux grandes catégories reviennent tout du long. Le code **logique pur** (maths, sélection de palier, formatage de message, catalogue statique...) ne dépend d'aucune classe Android et se teste directement en JVM -- c'est le cœur de la couverture actuelle, et la priorité de toute nouvelle fonction de ce type. Le code **lié au framework Android** (capteurs, caméra, MediaPipe natif, DataStore, Compose) a besoin d'un appareil/émulateur (test instrumenté) ou de Robolectric pour simuler l'environnement Android -- non couvert, la raison précise est indiquée à chaque fois plutôt qu'un simple "non testé".

## tracking/

| Fichier | Élément couvert | Test(s) | Statut |
|---|---|---|---|
| `RotationMath.kt` | `multiply`, `transpose`, `rotation3x3FromColumnMajor4x4`, `toEulerDegrees`, `mirrorEulerDegrees`, `composeCalibratedEuler`, `rotation3x3FromQuaternion` | `RotationMathTest.kt` | ✅ Couvert (15 tests) -- c'est la classe la plus délicate de l'app (risque connu d'inversion d'axes), priorité n°1. `rotation3x3FromQuaternion` est la brique préparatoire à la fusion ARCore : convertit le quaternion `Pose#getRotationQuaternion()` d'ARCore vers le même format 3x3 que le reste du pipeline. `toEulerDegrees` ne mirrore pas le yaw (comportement natif/anatomique) -- `mirrorEulerDegrees` porte le mode miroir explicitement, testé en isolation. |
| `TrackingTier.kt` | `TrackingTierSelector.select()` (dont le paramètre `override`) | `TrackingTierSelectorTest.kt` | ✅ Couvert (fonction pure `DeviceCapabilities -> TierConfig` ; `override` non-nul court-circuite la sélection automatique, `override` nul se comporte comme son absence -- forçage de palier depuis Diagnostics). |
| `BlendshapeCatalog.kt` | `all`, `byCategory`, `unreliable` | `BlendshapeCatalogTest.kt` | ✅ Couvert (cohérence structurelle : 52 entrées, pas de doublon, recouvrement catégorie/liste complète, noms de `unreliable` bien présents dans `all`). |
| `FaceLandmarkerHelper.kt` | `computeEyeGazeDegrees()` (extraite en `internal`, pure) | `FaceLandmarkerHelperTest.kt` | ✅ Couvert. |
| `FaceLandmarkerHelper.kt` | `setup()`, `tryCreateLandmarker()`, `detectAsync()`, `onLiveStreamResult()` (dont l'extraction de `faceLandmarks()`) | -- | ❌ Non testable en JUnit pur : enrobe le moteur natif MediaPipe (`FaceLandmarker.createFromOptions`, délégué GPU/CPU réel) -- nécessite un appareil/émulateur. `FaceLandmarkerResult` ne peut pas non plus être construit à la main en test (fabrique `create()` package-private côté MediaPipe). |
| `FaceTrackingResult.kt` | Data class (`FaceTrackingResult`, `BlendshapeScore`) | -- | ➖ Pas de logique propre, sert de fixture aux autres tests (voir `IFacialMocapSenderTest`, `FaceLandmarkerHelperTest`). |
| `ThermalThrottle.kt` | `ThermalThrottleState.next()` | `ThermalThrottleTest.kt` | ✅ Couvert (fonction pure, aucune dépendance de timing réel -- réduction/remontée du débit, plancher, seuil de sondages consécutifs pour `downgradeSuggested`, comportement sticky, rafales courtes qui ne déclenchent pas la suggestion). |
| `CalibrationAnomaly.kt` | `CalibrationAnomalyState.next()`, `maxAbsEulerDegrees()` | `CalibrationAnomalyTest.kt` | ✅ Couvert (fonction pure, aucune dépendance de timing réel -- comportement sticky, les deux signaux (repos+dérive, redétection de visage), gating repos/pose, rafales courtes qui ne déclenchent pas l'anomalie, expression active vs immobilité réelle, redétection indépendante du repos). |
| `BlendshapeStability.kt` | `meanAbsoluteBlendshapeDelta()` | `BlendshapeStabilityTest.kt` | ✅ Couvert (fonction pure -- sentinelle si comparaison impossible, delta correct, appariement par nom plutôt que par position, robuste à un ordre différent entre les deux listes). |
| `EyeAspectRatio.kt` | `eyeAspectRatio()`, `eyeAspectRatioFromLandmarks()` | `EyeAspectRatioTest.kt` | ✅ Couvert (fonction pure -- œil ouvert dans la plage attendue, œil fermé proche de zéro, garde division par zéro si coins confondus, lecture des bons indices depuis une liste de landmarks, liste trop courte -> 0). Utilisé par `EyeBlinkCorrection.kt`. |
| `EyeBlinkCorrection.kt` | `earOpenness()`, `correctBlinkScore()`, `correctEyeBlinkScores()` | `EyeBlinkCorrectionTest.kt` | ✅ Couvert (10 tests -- atténuation d'une fuite gauche/droite vs passage inchangé d'une vraie fermeture, orchestration bout en bout dont une tenue face à une dérive réaliste, et un clignement réel à angle de caméra inhabituel qui s'améliore après quelques épisodes). Corrige le blendshape `eyeBlinkLeft/Right` avant tout envoi réseau. Le lissage temporel repose sur `OneEuroFilter.kt` ci-dessous. La référence "fermé" est auto-adaptative (`AdaptiveEarFloor`, ligne suivante). |
| `EyeBlinkCorrection.kt` | `AdaptiveEarFloor.next()` -- référence EAR "fermé" auto-adaptative par œil | `AdaptiveEarFloorTest.kt` | ✅ Couvert (6 tests -- valeur initiale, aucun changement sous le seuil d'activité sans épisode en cours, minimum retenu sur tout l'épisode même si l'EAR remonte en cours de route, mélange partiel à la fin de l'épisode, convergence sur plusieurs épisodes vers le minimum réel, épisode isolé pollué qui ne redéfinit pas la référence à lui seul). Corrige un clin d'œil réel écrasé à un angle de caméra en contre-plongée. |
| `BrowRaise.kt` | `browRaiseRatio()`, `browRaiseRatioFromLandmarks()` -- même principe que l'EAR appliqué aux sourcils | `BrowRaiseTest.kt` | ✅ Couvert (5 tests -- sourcil levé > abaissé, calcul sur géométrie connue, coins d'œil confondus -> zéro, lecture des bons indices, liste trop courte -> zéro). Correspondance gauche/droite des indices ([BrowLandmarkIndices]) déduite par analogie, **pas encore confirmée sur device** -- diagnostic temporaire en place (`BrowDiag`). |
| `Mirroring.kt` | `mirrorBlendshapeName()`, `mirrorBlendshapes()`, `mirrorFaceTrackingResult()` -- mode miroir (tête + blendshapes gauche/droite + regard par œil) | `MirroringTest.kt` | ✅ Couvert (8 tests -- échange de noms, blendshapes/`FaceTrackingResult` sans paire inchangés, couverture systématique du vrai catalogue, involution). Garantit que le mode miroir applique tête et blendshapes gauche/droite de façon cohérente entre eux. |
| `OneEuroFilter.kt` | Filtre passe-bas à coupure adaptative (`minCutoff`, `beta`, `dCutoff`) | `OneEuroFilterTest.kt` | ✅ Couvert (8 tests -- premier échantillon non lissé, signal constant stable, saut isolé amorti, `beta` élevé réduit le retard sur un mouvement soutenu, `minCutoff` bas lisse davantage un signal bruité, convergence sur une cible tenue). Intégré dans `EyeBlinkCorrection.kt`. |
| `TongueOutGate.kt` | `jawOpenGateOpen()`, `mouthGeometricGateOpen()` -- étage 1 de la cascade langue tirée | `TongueOutGateTest.kt` | ✅ Couvert (9 tests -- sous/à/au-dessus du seuil, bornes 0f/1f, pour les deux portes). `jawOpenGateOpen()` est la seule porte qui bloque effectivement l'étage 1 -- fiable sur les paliers CameraX et ARCore. `mouthGeometricGateOpen()` existe et reste testée mais sert uniquement au diagnostic : une vraie tenue "langue dehors" peut produire la même signature géométrique qu'une bouche pressée (chevauchement quasi total), ce cas est tranché par l'étage 3 plutôt que par cette porte. |
| `LipLandmarks.kt` | `mouthOpennessRatio()`, `mouthCropRegion()` | `LipLandmarksTest.kt` | ✅ Couvert (9 tests -- géométrie connue, coins confondus -> null/0, liste trop courte, dimensions image invalides, clamp aux bornes, garde-fou `MIN_CROP_DIMENSION_PX`). `LipLandmarkIndices` est confirmé correct sur device (coordonnées brutes loguées et comparées). `MIN_CROP_DIMENSION_PX` (garde-fou dégénéré) reste en place mais n'est plus la ligne de défense principale contre le faux positif "bouche pressée" -- voir `mouthGeometricGateOpen()` ci-dessus. |
| `MouthColorAnalysis.kt` | `rgbToHsv()`, `isTongueColoredPixel()`, `tonguePixelRatio()`, `colorGateOpen()` -- étage 2 | `MouthColorAnalysisTest.kt` | ✅ Couvert (18 tests, valeurs HSV dérivées à la main). Mécanique fonctionnelle sur device (CameraX + ARCore) mais le classifieur couleur lui-même **non fiable** en pratique (seuil dérive trop d'une session à l'autre) -- voir `TongueColorBaseline.kt` ci-dessous. Les fonctions restent pures/testées/loguées, mais `colorGateOpen()`/`adaptiveFired` ne conditionnent plus le déclenchement de l'étage 3 -- diagnostic uniquement. |
| `TongueColorBaseline.kt` | `TongueColorBaseline.next()`, `isElevated()` -- référence "sans langue" adaptative (même principe qu'`AdaptiveEarFloor`) | `TongueColorBaselineTest.kt` | ✅ Couvert (6 tests -- premier échantillon, dérive EMA en zone normale, pas de dérive en zone élevée, `isElevated` sous/au-dessus de la marge). Améliore la discrimination par rapport à un seuil fixe sans la rendre fiable. Idem `MouthColorAnalysis.kt` ci-dessus : n'est plus utilisé comme filtre bloquant, seulement en diagnostic. |
| `InferenceLoadMonitor.kt` | `InferenceLoadState.next()` (EMA), `isRunningHigh()` | `InferenceLoadMonitorTest.kt` | ✅ Couvert (6 tests -- premier échantillon brut, convergence EMA, `isRunningHigh` sous/au-dessus de la marge, faux avant le premier échantillon). Alimente l'avertissement de charge de la détection langue tirée. |
| `TongueEmbeddingClassifier.kt` | `cosineSimilarity()`, `classifyTongueState()` -- étage 3 | `TongueEmbeddingClassifierTest.kt` | ✅ Couvert (9 tests -- vecteurs identiques/orthogonaux/opposés, vecteur nul sans NaN, classification prend la référence la plus proche, `UNDECIDED` si référence vide ou écart sous la marge). Le chevauchement "langue rentrée + mouvement de mâchoire" (risque de faux `TONGUE_OUT`) est atténué par une calibration élargie (`TongueCalibrationRecordingState.kt`), le résidu restant est filtré par l'anti-rebond d'injection (`TongueOutInjectionGate.kt`) -- injection réseau active. |
| `TongueEmbeddingHelper.kt` | `setup()`, `embed()` (GPU/CPU, `RunningMode.IMAGE`) | -- | ❌ Non testable en JUnit pur : enrobe `ImageEmbedder` natif MediaPipe, même raison que `FaceLandmarkerHelper.setup()` ci-dessus. |
| `TongueCalibrationAveraging.kt` | `EmbeddingAccumulator.accumulate()`, `average()` | `TongueCalibrationAveragingTest.kt` | ✅ Couvert (5 tests -- échantillon unique, moyenne par dimension dérivée à la main, zéro échantillon -> null, taille incohérente -> exception). |
| `TongueCalibrationRecordingState.kt` | `tick()`, `result()` -- machine à état de calibration (`PREPARE_TONGUE_OUT` → `RECORDING_TONGUE_OUT` → `PREPARE_TONGUE_IN` → `RECORDING_TONGUE_IN` → `DONE`) | `TongueCalibrationRecordingStateTest.kt` | ✅ Couvert (20 tests -- démarrage, accumulation isolée par phase, les deux phases `PREPARE_*` n'accumulent jamais, transitions aux frontières exactes de chaque phase (`prepareDurationMs`/`durationMs`/`tongueInDurationMs`), `result()` avant/après `DONE`, no-op après `DONE`/sur `IDLE`, valeurs par défaut). Les deux phases d'enregistrement (langue dehors, langue rentrée) sont précédées d'une pause de préparation (`PREPARE_TONGUE_OUT`/`PREPARE_TONGUE_IN`, `DEFAULT_CALIBRATION_PREPARE_DURATION_MS` partagé, 3000ms) pour laisser le temps de prendre la pose avant que l'enregistrement démarre. Chaque phase d'enregistrement applique son propre multiplicateur de durée (`TONGUE_OUT_RECORDING_MULTIPLIER`, `TONGUE_IN_RECORDING_MULTIPLIER`) pour couvrir plusieurs cycles de mouvement pendant la calibration. |
| `TongueOutInjectionGate.kt` | `TongueOutInjectionState.next()`, `.confirmed()`, `tongueOutInjectionShouldUpdate()` -- anti-rebond avant injection réseau (exige plusieurs `TONGUE_OUT` consécutifs) | `TongueOutInjectionGateTest.kt` | ✅ Couvert (15 tests -- état initial, incrément/reset par type de classement, seuil par défaut et personnalisé, reproduction de deux formes de résidu (une frame isolée, une paire consécutive) insuffisantes pour confirmer, tenue soutenue confirmée en continu). Permet l'activation de l'injection réseau malgré un résidu de faux positifs isolés. `tongueOutInjectionShouldUpdate()` (+5 tests) garantit que le compteur n'avance que sur les frames où une classification a effectivement eu lieu -- le throttle d'`embed()` peut sauter une frame caméra sans classification. |
| `TongueOutDisplaySmoothing.kt` | `TongueOutDisplayState.next()` -- lissage de la valeur affichée localement ET réellement injectée au réseau (compte-rebours de maintien après la dernière détection positive, alimenté par le signal déjà debouncé par `TongueOutInjectionGate.kt`, pas la classification brute) | `TongueOutDisplaySmoothingTest.kt` | ✅ Couvert (9 tests -- état initial, maintien tant que le délai n'est pas écoulé, expiration, accumulation sur plusieurs frames non positives, réinitialisation par un nouveau positif, `TONGUE_IN`/`UNDECIDED`/`null` traités identiquement comme non positifs). Évite un clignotement rapide 1/0 pendant une tenue -- ne touche à aucune décision de la cascade elle-même (fonction pure inchangée), seulement à ce qui lui est passé en entrée. |

## logging/

| Fichier | Élément couvert | Test(s) | Statut |
|---|---|---|---|
| `LogFormatting.kt` | `formatLogLine()`, `maskIpAddresses()`, `appendWithRotation()`, `shouldPersist()` | `LogFormattingTest.kt` | ✅ Couvert (13 tests -- format de ligne avec/sans pile d'appel, masquage IPv4 simple/intégré à un message/multiple/absent, rotation sous et au-delà de la limite de taille (garde les entrées récentes, jamais de ligne partielle en tête), filtrage par seuil de niveau). |
| `LogLevel.kt` | Enum (`VERBOSE`...`ERROR`, ordinal) | Exercé indirectement par `LogFormattingTest.kt` (`shouldPersist`) | ➖ Pas de logique propre à isoler, aucune valeur invalide possible pour un enum Kotlin. |
| `AppLog.kt` | `v()`/`d()`/`i()`/`w()`/`e()` (sink réel : `android.util.Log` + fichier) | -- | ❌ Dépend de `BuildConfig.DEBUG`, `android.util.Log` et l'accès disque réel -- même catégorie que le reste du glue code Android de ce projet. Toute la logique à risque (formatage, masquage, rotation, filtrage par niveau) est déjà couverte séparément dans `LogFormatting.kt` ; `AppLog` ne fait qu'orchestrer ces fonctions pures avant d'écrire. |

## network/

| Fichier | Élément couvert | Test(s) | Statut |
|---|---|---|---|
| `IFacialMocapSender.kt` | `toIFacialMocapName()`, `buildMessage()` (extraites en `internal`, pures) | `IFacialMocapSenderTest.kt` | ✅ Couvert -- ce format est à risque de bug silencieux en cas de désaccord de nommage (ex. `eyeSquint`) avec le protocole attendu côté récepteur. |
| `IFacialMocapSender.kt` | `startListening()`, `send()`/`openSendSocket()` (partie socket), `stopListening()` | -- | ❌ Dépend d'un vrai `DatagramSocket`/thread réseau -- nécessite un test instrumenté ou un socket factice. Non prioritaire, la partie à risque (le format du message) est déjà couverte séparément. Inclut la détection de déconnexion (`connect()`/`startUdpLivenessProbe`), best-effort et non fiable selon le réseau (voir `NetworkUtils.kt` ci-dessous), qui n'affecte jamais l'envoi lui-même. |
| `VmcOscSender.kt` | `buildBundle()`, `serializeToBytes()` (extraites `internal`, pures) | `VmcOscSenderTest.kt` | ✅ Couvert -- `buildBundle()` regroupe les messages `/Val` + `/Apply` en un seul `OSCBundle`. `serializeToBytes()` contourne un `NoSuchMethodError` d'`OSCPortOut` sur Android 11/API 30 : testée en round-trip complet, sérialise un bundle puis le reparse avec `OSCParser` et compare le contenu -- preuve en JVM que le remplacement produit des octets valides, pas seulement qu'il compile. |
| `VmcOscSender.kt` | `send()` (partie socket), `connect()` | -- | ❌ Dépend d'un vrai `DatagramSocket`/UDP -- nécessite un test instrumenté ou un socket factice. La partie à risque (le contenu du bundle ET sa sérialisation en octets) est déjà couverte séparément via `serializeToBytes()`. Même remarque détection de déconnexion que `IFacialMocapSender.kt` ci-dessus. |
| `NetworkUtils.kt` | `getLocalIpAddress()` | -- | ❌ Dépend des interfaces réseau réelles de l'appareil (`NetworkInterface.getNetworkInterfaces()`) -- environnement-dépendant, faible valeur à mocker pour une simple lecture d'IP locale. |
| `NetworkUtils.kt` | `startUdpLivenessProbe()` (sonde `receive()` de détection de déconnexion, partagée `VmcOscSender`/`IFacialMocapSender`) | -- | ❌ Dépend d'un vrai `DatagramSocket`/thread -- même limite que le reste de ce module. Son effet (détecter une cible devenue injoignable) ne se déclenche pas de façon fiable selon le réseau -- deux mécanismes de surfaçage sont en place (`send()` puis cette sonde `receive()`), aucun des deux ne voit systématiquement l'erreur ICMP. Best-effort assumé, ne bloque jamais l'envoi réel. |
| `VTubeStudioProtocol.kt` | Encodage/décodage JSON de chaque message de l'API Plugin VTube Studio (auth, création de paramètre, injection) -- pur, kotlinx.serialization | `VTubeStudioProtocolTest.kt` | ✅ Couvert. Inclut le décodage de vrais payloads serveur (copiés depuis la doc officielle) et la tolérance aux champs inconnus (`ignoreUnknownKeys`). |
| `VTubeStudioConnectionState.kt` | `nextVTubeStudioConnectionState()` (machine à état pure) | `VTubeStudioConnectionStateTest.kt` | ✅ Couvert. Toutes les transitions valides, plus les événements hors séquence (ignorés sans planter) et `Disconnect`/`SocketFailed` valides depuis n'importe quel état. |
| `VTubeStudioSender.kt` | Socket WebSocket réel (nv-websocket-client), pilotage de la machine à état en réaction aux messages entrants | -- | ❌ Dépend d'un vrai `WebSocket`/serveur VTube Studio -- même limite que `VmcOscSender.send()`/`connect()`. La partie à risque (protocole JSON, transitions d'état) est déjà couverte séparément. |

## camera/

| Fichier | Élément couvert | Test(s) | Statut |
|---|---|---|---|
| `CameraController.kt` | `rotatedDimensions()` (extraite `internal`, pure) | `CameraControllerTest.kt` | ✅ Couvert. |
| `CameraController.kt` | `bindImageAnalysis()`, `bindPreview()`, `processFrame()`, `acquirePooledBitmap()`, `releaseFrame()`, `peekPooledBitmap()` | -- | ❌ Dépend de CameraX (`ProcessCameraProvider`, `ImageProxy`) et `android.graphics.Bitmap/Canvas/Matrix` -- nécessite un test instrumenté (CameraX fournit un `CameraXConfig` de test, mais ça reste un test sur device/émulateur) ou Robolectric avec ses shadows graphiques. Le pool de bitmaps est un point sensible (comparaison par identité `MPImage` plutôt que par valeur) : bon candidat à un test instrumenté si l'app grossit encore sur ce point. `peekPooledBitmap()` alimente l'étage 2 de la cascade langue tirée sur palier CameraX. |
| `ArCoreHeadPoseTracker.kt` | Session ARCore, conversion YUV->Bitmap, `peekLastBitmap()`, `releaseFrame()`, fond caméra live GL (`setupBackgroundProgram`/`drawCameraBackground`) | -- | ❌ Dépend d'ARCore (`Session`, `Frame`) et `GLSurfaceView.Renderer` -- même limite que `CameraController.kt`, nécessite un test instrumenté ou une vérification manuelle sur device. `peekLastBitmap()`/`releaseFrame()` donnent à l'étage 2 de la cascade langue tirée un accès pixel sur palier ARCore. Le fond caméra live (quad GL texturé, shader/buffers/draw call) est du même type de code -- compilation de shader et appels `glDraw*` non exerçables en JVM ; `backgroundQuadCoords` est un simple quad figé (`val`), la rotation caméra fixe fonctionne correctement dans toutes les orientations physiques testées (portrait, paysage dans les deux sens, tête en bas). |

## capabilities/

| Fichier | Élément couvert | Test(s) | Statut |
|---|---|---|---|
| `DeviceCapabilities.kt` | `DeviceCapabilities.looksHighEnd` | Exercée indirectement par `TrackingTierSelectorTest.kt` | ⚠️ Couverte en pratique (les tests de sélection de palier construisent des `DeviceCapabilities` et vérifient le palier qui en résulte), mais pas de test dédié isolé sur `looksHighEnd` seul. À ajouter si cette heuristique devient plus complexe. |
| `DeviceCapabilities.kt` | `DeviceCapabilityDetector.detect()`, `isThermalThrottling()` | -- | ❌ Dépend de `ArCoreApk`, `ActivityManager`, `PowerManager` -- nécessite Robolectric ou un test instrumenté. Appelée en continu (`MainViewModel.startThermalPolling()`) ; la logique qui exploite son résultat (réduction/remontée du débit) est extraite en fonction pure et testée séparément, voir `ThermalThrottle.kt` ci-dessous. |

## sensors/

| Fichier | Élément couvert | Test(s) | Statut |
|---|---|---|---|
| `DeviceOrientationTracker.kt` | `rotationDeltaMatrix()`, `snapshotRotationMatrix()`, `start()`/`stop()` | -- | ❌ Dépend de `SensorManager`/`SensorEvent` réels pour être exercées de bout en bout. La partie mathématique (multiplication/transposition) qu'elles délèguent à `RotationMath` est, elle, déjà couverte par `RotationMathTest`. Faible valeur à mocker `SensorManager` juste pour ce glue code. `start()`/`stop()` sont appelés par `MainViewModel` sur `ON_START`/`ON_STOP` du cycle de vie plutôt qu'une seule fois à l'initialisation -- même remarque, glue code non testable sans Robolectric. |
| `IconOrientationTracker.kt` | `onOrientationChanged()` | -- | ❌ Wrapper direct d'`OrientationEventListener` (Android), aucune logique propre à isoler. |
| `BatteryMonitor.kt` | `onReceive()` | -- | ❌ Wrapper direct de `BroadcastReceiver`/`ACTION_BATTERY_CHANGED`, nécessite un test instrumenté pour simuler un vrai broadcast. |

## settings/

| Fichier | Élément couvert | Test(s) | Statut |
|---|---|---|---|
| `AppSettingsStore.kt`, `ConnectionSettingsStore.kt` | Getters/setters DataStore (dont `faceMeshOverlayEnabled`, `persistBlendshapeSelectionEnabled`, `persistedBlendshapeSelectionNames`, `debugForceArCoreUnavailable`, `debugForceGpuUnavailable`, `tongueOutDetectionEnabled`, `tongueReferencesCalibrated`, `tongueCalibrationRecordingDurationMs`, `tongueClassificationMargin`) | -- | ❌ Dépendent de `Context`/DataStore Preferences -- nécessitent Robolectric (avec un `PreferenceDataStore` en mémoire) ou un test instrumenté. Bon candidat pour une prochaine itération : la logique (lire une clé, valeur par défaut si absente, écrire) est simple mais entièrement non couverte aujourd'hui. |
| `TongueCalibrationStore.kt` | `load()`, `save()`, `clear()` -- persistance CSV des références de calibration, étage 3 | `TongueCalibrationStoreTest.kt` | ✅ Couvert (5 tests -- ne dépend que de `java.io.File`, testable en JVM pur malgré son package `settings/`, contrairement à `AppSettingsStore`/DataStore). `save()` formate les floats avec `Locale.ROOT` explicite -- sans ça, sous locale française, la virgule décimale collisionne avec le séparateur CSV et double la taille du vecteur relu, cassant silencieusement la comparaison de similarité (`simOut`/`simIn` bloqués à 0.0, `UNDECIDED` permanent) après tout redémarrage de l'app. Test de non-régression sous locale française inclus. |
| `AppLanguage.kt` | `appLanguageFromTag()` (mapping tag/enum pur) | `AppLanguageTest.kt` | ✅ Couvert. La construction du `LocaleListCompat` réel et l'appel `AppCompatDelegate.setApplicationLocales()` (côté `MainViewModel.setAppLanguage()`) restent non testés, même limite que les autres points de contact avec le framework Android listés dans ce document. |

## ui/ (Compose)

| Fichier | Élément couvert | Test(s) | Statut |
|---|---|---|---|
| `MainViewModel.kt` | Orchestration (caméra, capteurs, réseau, DataStore, minuteur éco...) | -- | ❌ Dépend d'`AndroidViewModel`/`Application` et de tous les composants ci-dessus. Volontairement laissé "fin" : sa seule logique non triviale (composition de la calibration) a été extraite dans `RotationMath.composeCalibratedEuler`, testée séparément -- stratégie à reconduire pour toute nouvelle logique ajoutée au ViewModel. |
| `MainViewModel.kt` | `initializeTracking()` idempotent (`trackingInitialized`) | -- | ❌ Un test pertinent demanderait d'instancier un `MainViewModel` réel (Application, ArCoreApk, ActivityManager...) et d'appeler `initializeTracking()` deux fois -- nécessite Robolectric. Garde à une ligne, risque de régression faible ; à couvrir si Robolectric est introduit pour d'autres besoins (ex. `AppSettingsStore`). |
| `MainScreen.kt` | Rattachement d'`IconOrientationTracker`/`BatteryMonitor` au cycle de vie (`ON_START`/`ON_STOP`) | -- | ❌ Composable Compose observant un vrai `Lifecycle` -- nécessite un test d'UI Compose ou Robolectric pour simuler des transitions de cycle de vie. Vérifié manuellement. |
| `MainScreen.kt`, `MainHud.kt`, `SettingsScreen.kt`, `DiagnosticsScreen.kt`, `ConnectionSettingsScreen.kt`, `DisplaySettingsScreen.kt`, `ExperimentalFeaturesScreen.kt`, `LoggingSettingsScreen.kt`, `BlendshapeSelectionScreen.kt`, `BlendshapePanel.kt`, `PowerSaveOverlay.kt`, `LowBatteryAlert.kt`, `FaceMeshOverlay.kt`, `Theme.kt` | Composables | -- | ❌ Hors périmètre des tests unitaires JVM : nécessitent soit des tests d'UI Compose (`androidx.compose.ui.test`, instrumentés ou Robolectric), soit une vérification manuelle. |
| `LandmarkProjection.kt` | `toScreenPoint()` (projection normalisé -> écran, recadrage centré type `PreviewView` quand le ratio image/canvas diffère, avec/sans miroir, repli si dimensions image inconnues) | `LandmarkProjectionTest.kt` | ✅ Couvert -- seule partie mathématique de l'overlay du mesh (voir `FaceMeshOverlay.kt` ci-dessus, qui l'utilise mais reste lui-même hors périmètre JUnit). Gère un ratio écran/image caméra différent (ex. téléphones dont l'écran a un ratio inhabituel) pour que le mesh ne se décale/écrase pas à l'écran. |
| `DebugPanelUnlock.kt` | `DebugPanelUnlockState.registerTap()` | `DebugPanelUnlockTest.kt` | ✅ Couvert (fonction pure, aucune dépendance Android -- comptage de taps consécutifs dans une fenêtre de temps, reset hors fenêtre, borne inclusive, no-op une fois déverrouillé). |
| `RotationBucket.kt` | `snapToRotationBucket()` | `RotationBucketTest.kt` | ✅ Couvert (6 tests -- bornes des 4 paliers {0,90,180,270} et normalisation des valeurs hors [0,360)). Utilisée pour la rotation cosmétique du panneau de blendshapes (`MainScreen.kt`). |

## Racine

| Fichier | Élément couvert | Test(s) | Statut |
|---|---|---|---|
| `MainActivity.kt`, `MoCapApplication.kt` | Cycle de vie Android (`MoCapApplication.onCreate()` initialise aussi `AppLog` -- un appel direct sans logique propre à isoler) | -- | ❌ Points d'entrée Android purs, testables uniquement via test instrumenté. |

## Bilan actuel

35 fichiers de test, tous en JVM pur (aucun appareil/émulateur requis) : `RotationMathTest`, `TrackingTierSelectorTest`, `BlendshapeCatalogTest`, `FaceLandmarkerHelperTest`, `IFacialMocapSenderTest`, `CameraControllerTest`, `VmcOscSenderTest`, `LandmarkProjectionTest`, `ArCoreFaceSelectorTest`, `MeshOverlayVisibilityTest`, `ThermalThrottleTest`, `DebugPanelUnlockTest`, `CalibrationAnomalyTest`, `BlendshapeStabilityTest`, `VTubeStudioProtocolTest`, `VTubeStudioConnectionStateTest`, `AppLanguageTest`, `EyeAspectRatioTest`, `EyeBlinkCorrectionTest`, `OneEuroFilterTest`, `AdaptiveEarFloorTest`, `LogFormattingTest`, `BrowRaiseTest`, `MirroringTest`, `TongueOutGateTest`, `LipLandmarksTest`, `MouthColorAnalysisTest`, `TongueColorBaselineTest`, `InferenceLoadMonitorTest`, `TongueEmbeddingClassifierTest`, `TongueCalibrationAveragingTest`, `TongueCalibrationRecordingStateTest`, `TongueCalibrationStoreTest`, `TongueOutDisplaySmoothingTest`, `TongueOutInjectionGateTest`. Ils couvrent l'intégralité de la logique mathématique et de formatage identifiée comme la plus fragile (rotation/calibration -- dont le mode miroir, mapping de noms de blendshapes, regard des yeux, sélection de palier (dont son forçage manuel), dimensions de rotation caméra, regroupement des messages OSC, projection écran du mesh, sélection du visage principal ARCore, visibilité de l'overlay, réponse au throttling thermique, déverrouillage du panneau de mocks de debug, détection d'anomalie de calibrage, protocole et machine à état de l'API Plugin VTube Studio, mapping de la langue en-app, Eye Aspect Ratio et sa correction anti-fuite/lissage pour la fiabilisation du clignement, filtre à coupure adaptative générique, référence EAR "fermé" auto-adaptative par œil, formatage/masquage/rotation des logs, hauteur géométrique des sourcils, cascade de détection de la langue tirée -- porte jawOpen, recadrage buccal, analyse couleur, référence couleur adaptative, moniteur de charge d'inférence, classification par embedding et calibration personnelle de l'étage 3), en s'appuyant sur quelques extractions de fonctions pures (visibilité `internal`) qui ne changent aucun comportement.

## Overlay du mesh de tracking (option activable)

Superposition optionnelle des 478 points du mesh facial MediaPipe sur l'aperçu caméra, activable/désactivable depuis les réglages (persisté). Suit exactement le même principe que le reste de l'app : la seule partie mathématique -- la projection d'un point normalisé vers l'espace écran, avec effet miroir pour correspondre à l'aperçu -- a été écrite en TDD (`LandmarkProjectionTest.kt` avant `LandmarkProjection.kt`) et isolée du Composable qui l'utilise (`FaceMeshOverlay.kt`, non testable en JUnit pur).

Point d'attention pour la vérification manuelle sur device : la projection suppose que le calque occupe tout l'écran au même rapport largeur/hauteur que l'image analysée par MediaPipe, sans compenser un éventuel recadrage de `PreviewView` -- si les points ne se superposent pas exactement au visage à l'écran (léger décalage ou mise à l'échelle), c'est le premier endroit à regarder.

## Cycle de vie et double-appel d'initialisation -- pas de test dédié, raison assumée

Deux sujets -- capteurs/batterie pas alignés sur le cycle de vie, et `initializeTracking()` pas
protégé contre un double appel -- ne contenaient aucune logique pure à extraire -- uniquement du
glue code Android (observation de `Lifecycle`, garde sur un booléen d'instance). Implémentés
directement, sans étape rouge/verte JUnit au préalable, avec la raison documentée ci-dessus plutôt
qu'un silence. Vérification manuelle recommandée après relance de l'app : mettre l'app en
arrière-plan quelques secondes puis revenir dessus, et confirmer que le tracking reprend
normalement (capteurs bien redémarrés) sans redémarrage complet.

## Fusion ARCore

La brique de maths pure a été préparée en premier, comme le reste de l'app (extraire la logique à
risque en fonction pure, la couvrir en JUnit, avant de toucher au code Android qui l'entoure) :
`RotationMath.rotation3x3FromQuaternion()`, testée par deux cas (quaternion identité, et rotation de
90° autour de Z comparée à la matrice de rotation Z déjà utilisée ailleurs dans les tests).

Ce qui est couvert en JUnit :
- `ArCoreFaceSelector.pickPrimary()` -- sélection du visage principal parmi les candidats suivis,
  4 cas (`ArCoreFaceSelectorTest.kt`).
- `computeMeshOverlayVisible()` (`ui/MeshOverlayVisibility.kt`) -- table de vérité complète des 4
  entrées booléennes (ARCore actif, réglage overlay, mode éco, garder-en-éco), 8 cas
  (`MeshOverlayVisibilityTest.kt`).

Ce qui n'est **pas** couvert, volontairement, même raison que le reste du glue code Android de cette
app (`DeviceCapabilityDetector`, `CameraController`...) : `ArCoreHeadPoseTracker` dépend directement
du SDK ARCore (`Session`, `GLSurfaceView.Renderer`, `Frame.acquireCameraImage()`) et du branchement
caméra/cycle de vie dans `MainViewModel` -- nécessiterait Robolectric ou un test instrumenté pour un
gain limité vu la nature du code (orchestration, pas de décision non triviale isolable). Cette
absence de couverture JUnit ne reflète pas l'état de fonctionnement réel de la fonctionnalité :
la fusion ARCore est confirmée fonctionnelle sur device (voir `AndroidMoCap_spec_technique_FR.md`, §4)
-- c'est uniquement ce fichier de suivi de tests qui ne peut pas l'exercer automatiquement.

## Règle pour la suite (TDD)

Pour toute nouvelle fonctionnalité ou correction : 1) écrire d'abord le test qui décrit le comportement attendu (il doit échouer), 2) écrire le code minimal pour le faire passer, 3) mettre à jour ce document (nouvelle ligne dans le tableau du module concerné, ou nouvelle section). Si une fonction contient de la logique non triviale mais est mélangée à du code Android (comme c'était le cas pour la calibration ou le regard des yeux), l'extraire d'abord en fonction pure (`internal` si elle ne fait pas partie de l'API publique) avant de l'écrire -- c'est ce qui a permis de couvrir la quasi-totalité de la logique à risque de cette app sans passer par Robolectric ni test instrumenté.
